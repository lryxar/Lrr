services:
  bot-hosting-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot-hosting-api
    ports:
      - "8000:8000"
    volumes:
      - ./user_bots:/app/user_bots
      - ./bot_hosting.db:/app/bot_hosting.db
    restart: unless-stopped
